<a href="{{ path('taxe_user_edit', { 'id': user.id }) }}" role="button"
   class="btn btn-primary"><i
            class="ti ti-pencil"></i> Editer</a>

<a href="{{ path('taxe_user_password', { 'id': user.id }) }}" role="button"
   class="btn btn-primary"><i
            class="ti ti-lock"></i> Mot de passe</a>

<a href="{{ path('taxe_user') }}" class="btn btn-primary" role='button'><i class="ti ti-list"></i>
    Retour à la Liste</a>

<button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
    <i class="ti ti-trash"></i> Supprimer
</button>

<br /><br />
