{% extends '@Taxe/layout.html.twig' %}

{% block body %}

<div class="card">
    <div class="card-header">
        <h3>{{ user }}</h3>
    </div>
    <div class="card-body">

    {% include '@Taxe/user/_actions.html.twig' %}

    <br />
    <table class="table table-bordered table-hover">
        <tbody>
        <tr>
            <th>Nom prénom</th>
            <td>
                {{ user.nom }} {{ user.prenom }}
            </td>
        </tr>
        <tr>
            <th>Email</th>
            <td>
                {{ user.email }}
            </td>
        </tr>
        </tbody>
    </table>
    </div>
</div>

    {% include '@Taxe/user/_delete.html.twig' %}

{% endblock %}
