{{ form_start(form) }}
    {{ form_row(form.annee) }}
    {{ form_row(form.url) }}
    {{ form_row(form.file) }}
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
