<script>
    const btns = document.querySelectorAll('.btnDelete');
    Array.prototype.forEach.call(btns, function (el, i) {
        el.addEventListener('click', () => {
            const id = el.dataset.id;
            document.querySelector("#tokenauth").value = el.dataset.token;
            document.querySelector("#idexercice").value = id;
        });
    });
</script>
<div class="modal fade" id="deleteAuthorization" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel"
     aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title" id="myModalLabel">Supprimer</h4>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Ëtes vous sur de supprimer l'exercice ?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler
                </button>
                <form method="post" action="{{ path('exercice_delete') }}">
                    <input type="hidden" name="idexercice" id="idexercice" value="">
                    <input type="hidden" name="_tokenauth" id="tokenauth" value="">
                    <button type="submit" class="btn btn-danger">Supprimer</button>
                </form>
            </div>
        </div>
    </div>
</div>
